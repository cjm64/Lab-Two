package BackEnd.scalaClasses

class Projectile(x: Double, y: Double) {


  // note that I don't actually know the size of the projectile
  val sizeX: Double = 10.0
  val sizeY: Double = 10.0
  // this size might not be right, feel free to change it


  // the velocity is initially initialized to null and these values must be changed before using them
  var velocityX: Double = _
  var velocityY: Double = _


}
