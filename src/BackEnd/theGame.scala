package BackEnd

class theGame {
  // probably extends actor - no,
  // new game actor for each new player? - probably
  // this will have all of the methods, variables, and states that we need for gameActor






}
