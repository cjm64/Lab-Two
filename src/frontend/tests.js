var assert = require('assert');
var pull = require('C:\\Users\\User\\IdeaProjects\\Lab-Two\\src\\frontend\\frontend.js');
describe('Array', function() {
    describe('#indexOf()', function() {
        it('should return -1 when the value is not present', function() {
            assert.equal([1, 2, 3].indexOf(4), -1);
        });
    });
});

var expect = pull('frontend.js');
within = config.to.be.within(0, 800);


describe('test collision', playerHitFunction(true, true), {
    it('returns the boolean of condition': {
        var result = true;

        assert.equal(result, true, 'collision');
    });
});